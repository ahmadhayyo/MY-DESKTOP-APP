# خطة تطوير وكيل HAYO — الطريق إلى القمة

> وثيقة حيّة. الهدف: وكيل تنفيذي محلي **صلب ودقيق ومقتصد في التوكِن**، بالاستفادة
> الانتقائية من هندسة مشروع HackerAI (دون نقل بنيته السحابية).

## المبادئ الحاكمة
1. **محلي أولاً** — لا نقل لأي بنية سحابية (E2B / Trigger.dev / Convex / Stripe).
2. **الكلفة قيد تصميمي** — كل ميزة تُقاس بأثرها على استهلاك التوكِن.
3. **قيمة تدريجية** — كل مرحلة تنفع وحدها.
4. **لا كسر لما يعمل** — تدوير المفاتيح، ReAct، HITL، اتساع الأدوات، والمرونة
   (resilience) الحالية تبقى وتُحسَّن، لا تُستبدل.

---

## ✅ المرحلة 1 — نواة تفكير صلبة (مُنجَزة)
أعلى عائد، أُنجزت واختُبرت بالكامل (16/16 اختبار ناجح).

| الوحدة | الملف | الوظيفة |
|---|---|---|
| كشف حلقات التكرار | `core/loop_detection.py` | بصمة name+args مع تجريد الحقول التجميلية؛ تحذير عند 3 تكرارات متطابقة، إيقاف عند 5. |
| تقليم مخرجات الأدوات | `core/compaction.py` | ميزانية توكِن متدحرجة (40K): يبقي الحديث كاملاً، يستبدل القديم بسطر، يحمي أدوات الذاكرة/الملاحظات. |
| القياس | `core/agent_metrics.py` | يسجّل أحداث التقليم/الحلقات في `logs/agent_metrics.jsonl` (best-effort، لا يكسر الوكيل أبداً). |
| الاختبارات | `tests/test_phase1_robustness.py` | 16 اختبار (unittest، بلا pytest). |

**نقاط الدمج في `agent/nodes.py`:**
- `_summarize_old_messages`: تقليم المخرجات قبل أي تلخيص (يوفّر توكِن في كل دورة).
- `should_continue`: إيقاف نظيف عند halt بدل حرق الخمسين تكراراً.
- `worker_node`: حقن رسالة "غيّر النهج" عند مستوى التحذير (تُرسل للنموذج فقط، لا تُخزّن).

**التحكم عبر `config.py` / `.env`:**
`LOOP_WARNING_THRESHOLD=3` · `LOOP_HALT_THRESHOLD=5` ·
`TOOL_OUTPUT_TOKEN_BUDGET=40000` · `PRUNE_MIN_SAVINGS=8000` · `AGENT_METRICS=1`

**التشغيل والتحقق:**
```
venv\Scripts\python -m unittest tests.test_phase1_robustness
```

---

## اكتشاف مهم أثناء التنفيذ
مراجعة الكود كشفت أن **مرونة الاستدعاء موجودة وناضجة أصلاً**:
- `_safe_llm_invoke` (في `agent/nodes.py`): retry-with-backoff، تدوير مفاتيح عند 429،
  معالجة 413/401/403 برسائل عربية واضحة.
- `core/resilience.py`: يلفّ كل استدعاء أداة بتراجع أسّي وتصنيف عابر/دائم للأخطاء.

⟹ بنودٌ ظننتها ثغرات (retry، self-healing) **مُنفَّذة سلفاً**. لذلك حُذفت من الخطة،
ولا يجوز العبث بهذه المسارات دون اختبار على حِمل حقيقي.

---

## المرحلة 2 — فكّ العقد المستعصية (مقترحة — تحتاج حضورك للاختبار)
تُبنى كـ**سُلّم تصعيد** داخل `reviewer_node`؛ **متوسطة المخاطر** لأنها تمسّ مسار
النموذج/المزوّد، فلا تُنفَّذ دون حضورك لاختبارها على مهامك الفعلية.
1. إعادة صياغة النهج (مُنجزة جزئياً عبر نودج الحلقة).
2. **تصعيد النموذج**: الرخيص أولاً، ثم الأقوى عند الفشل المتكرر لهذه الخطوة فقط.
3. **REPLAN**: تحليل السبب الجذري من `error_logs` وتوليد خطة جديدة (الوسم موجود، يُفعّل).
4. **وكيل فرعي** بسياق نظيف للمشكلة الصعبة الواحدة.
5. بحث/توثيق عند الخطأ الغامض قبل الاستسلام.
6. تصعيد للإنسان (HITL موجود) كملاذ أخير.

## المرحلة 3 — كفاءة التكلفة
- موجّه نماذج (بسيط→رخيص/Ollama، معقّد→قوي).
- Ollama المجاني افتراضاً للمهام الخفيفة.
- سقف توكِن/تكلفة للجلسة.

## المرحلة 4 — الواجهة (Chainlit)
بثّ التفكير، بطاقات أدوات قابلة للطي، لوحة تقدّم، HITL أنيق. (واجهة ويب كاملة = مشروع منفصل لاحق.)

## المرحلة 5 — الذاكرة والموثوقية
ترقية `long_term_memory.py` (استرجاع أدق)، وصقل التعافي بعد الأعطال.

## المرحلة 6 — التحقق (مستمرة)
توسيع اختبارات `core/` وتشغيل مجموعة المهام الذهبية قبل كل دمج.

---

## ما لا يُنقَل من HackerAI
E2B · Trigger.dev · Convex · WorkOS · Stripe · rate-limiting السحابي ·
قوالب الـpentesting · مدراء الـsandbox — كلها معاكسة للتصميم المحلي.

## ملاحظة أمان
`.env` يحوي مفاتيح حقيقية — يبقى خارج git، ولا يُلتقط `.env.example` الخاص بـHackerAI فوقه.
